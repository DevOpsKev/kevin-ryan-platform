services:
  web:
    build:
      context: ../..
      dockerfile: sites/specmcp-ai/Dockerfile
    container_name: specmcp-ai
    ports:
      - "8083:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3
