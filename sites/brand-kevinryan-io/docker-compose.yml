services:
  web:
    build:
      context: ../..
      dockerfile: sites/brand-kevinryan-io/Dockerfile
    container_name: brand-kevinryan-io
    ports:
      - "8081:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/healthz"]
      interval: 10s
      timeout: 5s
      retries: 3
